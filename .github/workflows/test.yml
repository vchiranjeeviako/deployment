name: deployment status test

on:
  deployment_status

jobs:
  tests:
    steps:
      - name: Pending
        if: ${{ github.event.deployment_status["status"] == "pending" }}
        run: echo "pending"

      - name: Success
        if: ${{ github.event.deployment_status["status"] == "success" }}
        run: echo "success"

      - name: Failure
        if: ${{ github.event.deployment_status["status"] == "failure" }}
        run: echo "failure"

      - name: Error
        if: ${{ github.event.deployment_status["status"] == "error" }}
        run: echo "error"